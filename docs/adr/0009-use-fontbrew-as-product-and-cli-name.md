# Use Fontbrew as product and CLI name

The project uses Fontbrew as the product name and `fontbrew` as the CLI command. The name clearly communicates a Homebrew-like package manager for fonts and aligns with the existing repository name. Related local paths and product language should use `fontbrew` or `Fontbrew` consistently.
