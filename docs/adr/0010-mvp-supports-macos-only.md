# MVP supports macOS only

Fontbrew's MVP product commitment is macOS only. Font activation, conflict detection, and user font directories differ across operating systems, so the first version focuses on reliable behavior in `~/Library/Fonts/Fontbrew` while keeping platform-specific path and activation logic isolated enough for future adapters.
